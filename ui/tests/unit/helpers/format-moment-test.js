
import { formatMoment } from 'sch/helpers/format-moment';
import { module, test } from 'qunit';

module('Unit | Helper | format moment');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatMoment([42]);
  assert.ok(result);
});

