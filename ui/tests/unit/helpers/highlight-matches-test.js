
import { highlightMatches } from 'sch/helpers/highlight-matches';
import { module, test } from 'qunit';

module('Unit | Helper | highlight matches');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = highlightMatches([42]);
  assert.ok(result);
});

