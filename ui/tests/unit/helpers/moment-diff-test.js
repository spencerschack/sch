
import { momentDiff } from 'sch/helpers/moment-diff';
import { module, test } from 'qunit';

module('Unit | Helper | moment diff');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = momentDiff([42]);
  assert.ok(result);
});

